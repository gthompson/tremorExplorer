# Install environment with
# conda env create -f iceweb.yml
name: iceweb
channels:
  - conda-forge
dependencies:
  - python=3.10
  - cartopy
  - folium
  - ipython
  - jupyter
  - obspy=1.4
  - pandas
  - pytest
  - seaborn
  - plotly
  - streamlit
